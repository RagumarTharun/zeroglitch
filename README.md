# zeroglitch
This module is part of a self-learning tracking tool designed to measure a student's study presence during focused sessions (such as Pomodoro). The page uses WebRTC camera access and lightweight motion detection to estimate whether a user is present in front of the camera during a study session.
reedit for branch1
